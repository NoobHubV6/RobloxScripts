local lib = loadstring(Game:HttpGet("https://raw.githubusercontent.com/NoobHubV1/NoobHubV1/main/Wizard%20Lib.lua"))()
local Window = lib:NewWindow("+1 Power Per Jump")
local Main = Window:NewSection("Main")
local autoWin
Main:CreateToggle("Auto Win", function(state)
  autoWin = state
end)
spawn(function()
  task0 = function()
    if autoWin then
      game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = workspace.Persistent.Wins["10"].Touch.CFrame
      wait(.15)
      fireproximityprompt(workspace.Persistent.Wins["10"].Touch.WinBoard.Prompt.ProximityPrompt:FindFirstChild("ProximityPrompt"))
      wait(.3)
    end
  end
  while task.wait() do
    pcall(task0)
  end
end)
