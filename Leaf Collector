local lib = loadstring(Game:HttpGet("https://raw.githubusercontent.com/NoobHubV1/NoobHubV1/main/Wizard%20Lib.lua"))()
local Window = lib:NewWindow("Leaf Collector")
local Main = Window:NewSection("Main")
function GetStats(number, Stats)
  game.ReplicatedStorage.Remotes.CollectCurrency:FireServer("Collect",number,Stats,true)
end
Main:CreateButton("Max All Stats", function()
  GetStats(math.huge, "Leaves")
  GetStats(math.huge, "Apples")
  GetStats(math.huge, 'Rebirths')
  GetStats(math.huge, "Gold")
  GetStats(math.huge, 'Dollars')
  GetStats(math.huge, 'Taaffeite Apples')
  GetStats(math.huge, "Golden Apples")
end)
