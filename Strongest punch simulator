local lib = loadstring(Game:HttpGet("https://raw.githubusercontent.com/NoobHubV1/NoobHubV1/main/Wizard%20Lib.lua"))()
local Window = lib:NewWindow("Strongest punch simulator")
local Main = Window:NewSection("Main")
local connection, AutoOrb
Main:CreateToggle("Auto Punch", function(state)
  if state then
    connection = game.RunService.RenderStepped:Connect(function()
      game.ReplicatedStorage.RemoteEvent:FireServer({"Activate_Punch"})
    end)
  else
    connection:Disconnect()
    connection = nil
  end
end)
Main:CreateToggle("Auto Orb", function(state)
  AutoOrb = state
  while AutoOrb and wait(1) do
    for i,v in ipairs(game.Workspace.Map.Stages.Boosts:FindFirstChild(game.Players.LocalPlayer.leaderstats.WORLD.Value):GetChildren()) do
      if v["0"]:FindFirstChild("TouchInterest") then
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v["0"].CFrame
        wait(.375)
      end
    end
  end
end)
