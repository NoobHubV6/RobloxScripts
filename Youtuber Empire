local lib = loadstring(Game:HttpGet("https://raw.github.com/NoobHubV1/NoobHubV1/main/Wizard%20Lib.lua"))()
local Window = lib:NewWindow("Youtuber Empire")
local fastRecord
local Main = Window:NewSection("Main")
Main:CreateToggle("Fast Record", function(state)
  fastRecord = state
end)
spawn(function()
  task0 = function()
    if fastRecord then
      game.ReplicatedStorage.Shared.Remotes.RecordFinished:FireServer()
    end
  end
  while task.wait() do
    pcall(task0)
  end
end)
